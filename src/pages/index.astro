---
import Footer from "../components/footer.astro";
import Header from "../components/header.astro";
import Main from "../layouts/main.astro";
import experienceData from "../data/experience.yaml";
import focusData from "../data/focus.yaml";
import readingData from "../data/reading.yaml";

// YAML imports arrive untyped; these shapes mirror the files in src/data/.
interface Focus {
	title: string;
	note: string;
}

interface Experience {
	period: string;
	role: string;
	org: string;
	place: string;
	note: string;
	tags: string[];
	current?: boolean;
}

interface Reading {
	title: string;
	source: string;
	url: string;
	note: string;
}

const focus = focusData as Focus[];
const experience = experienceData as Experience[];
const reading = readingData as Reading[];

const title = "Anikesh G Kamath — Backend & Agentic AI Engineer";
const description =
	"Backend engineer in Santa Clara building API-first systems, cloud-native services on AWS, and agentic AI workflows that are robust and production-ready. Currently Senior Software Engineer at Lineaje.";

// Reading shows three entries up front; the rest sit behind a disclosure.
const VISIBLE_READING = 3;
const readingVisible = reading.slice(0, VISIBLE_READING);
const readingRest = reading.slice(VISIBLE_READING);
---

<Main title={title} description={description}>
	<Header />

	<main class="mx-auto max-w-3xl px-6">
		<section class="rise py-16 sm:py-24">
			<p class="label-eyebrow">Santa Clara, California</p>
			<h1
				class="mt-5 text-balance font-display text-5xl font-semibold leading-[1.05] tracking-tight sm:text-6xl"
			>
				Anikesh G Kamath
			</h1>

			<p class="mt-6 max-w-2xl text-pretty text-lg leading-relaxed">
				Backend engineer building API-driven systems and agentic AI workflows that turn
				complex data into action.
			</p>
			<p class="mt-3 text-[0.9375rem] text-muted-foreground">
				<span class="font-semibold text-foreground">Senior Software Engineer at Lineaje</span
				> · MSCS from Northeastern
			</p>

			<div class="mt-9 flex flex-wrap items-center gap-x-8 gap-y-3 text-sm font-medium">
				<a href="mailto:anikesh.kamath@gmail.com" class="link-underline">Email</a>
				<a
					href="https://www.linkedin.com/in/anikeshk/"
					target="_blank"
					rel="noreferrer"
					class="link-underline text-muted-foreground"
				>
					LinkedIn
				</a>
				<a
					href="https://github.com/anikeshk"
					target="_blank"
					rel="noreferrer"
					class="link-underline text-muted-foreground"
				>
					GitHub
				</a>
			</div>
		</section>

		<section id="about" class="rule-top py-16">
			<p class="label-eyebrow">About</p>
			<div class="mt-6 space-y-5 text-base leading-relaxed text-muted-foreground">
				<p>
					I started out on traditional machine learning systems — integrating classical
					models into production APIs and learning what it takes to run ML reliably at
					scale. To strengthen the systems side, I finished a Master&apos;s in Computer
					Science at Northeastern in May 2025, focused on distributed systems, backend
					engineering, and cloud-native architecture.
				</p>
				<p>
					Today I work at the intersection of backend APIs, large-scale data systems, and
					agentic AI: high-throughput services and workflow-driven automation that pair
					LLM reasoning with deterministic, production-grade engineering.
				</p>
				<p>
					Alongside the technical work I&apos;ve spent a lot of time on leadership,
					mentorship, and teaching at Northeastern, and was honored with the Society of
					Distinctions and the Emerging Graduate Student Leader Award. I&apos;m drawn to
					teams building backend platforms and agentic systems that are robust,
					explainable, and genuinely production-ready.
				</p>
			</div>
		</section>

		<section class="rule-top py-16">
			<p class="label-eyebrow">What I work on</p>
			<dl class="mt-8 grid gap-x-10 gap-y-8 sm:grid-cols-2">
				{
					focus.map((item) => (
						<div class="border-l-2 border-foreground pl-4">
							<dt class="font-display text-xl font-semibold tracking-tight">
								{item.title}
							</dt>
							<dd class="mt-2 text-[0.9375rem] leading-relaxed text-muted-foreground">
								{item.note}
							</dd>
						</div>
					))
				}
			</dl>
		</section>

		<section id="work" class="rule-top py-16">
			<p class="label-eyebrow">Experience</p>
			<ol class="mt-8 space-y-9">
				{
					experience.map((item) => (
						<li class:list={["step-item", item.current && "step-item-current"]}>
							<p class="label-meta">{item.period}</p>
							<h3 class="mt-1.5 font-display text-xl font-semibold leading-tight tracking-tight">
								{item.role}
							</h3>
							<p class="mt-1 text-sm font-semibold">
								{item.org} <span class="text-muted-foreground">· {item.place}</span>
							</p>
							<p class="mt-2 max-w-xl text-[0.9375rem] leading-relaxed text-muted-foreground">
								{item.note}
							</p>
							<p class="mt-2 text-sm text-muted-foreground">{item.tags.join(" · ")}</p>
						</li>
					))
				}
			</ol>
		</section>

		<section id="reading" class="rule-top py-16">
			<p class="label-eyebrow">Reading</p>
			<p class="mt-4 max-w-xl text-[0.9375rem] leading-relaxed text-muted-foreground">
				Engineering writing worth passing on — blogs and articles I keep coming back to.
			</p>

			{/* `note` is intentionally not rendered — add it back below `source` if wanted. */}
			<ul class="mt-8 border-t border-border">
				{
					readingVisible.map((item) => (
						<li class="border-b border-border py-4">
							<a
								href={item.url}
								target="_blank"
								rel="noreferrer"
								class="link-underline text-[0.9375rem] font-medium leading-snug"
							>
								{item.title}
							</a>
							<p class="label-meta mt-1.5">{item.source}</p>
						</li>
					))
				}
			</ul>

			{
				readingRest.length > 0 && (
					<details>
						<summary class="label-meta mt-6 inline-block border-b border-transparent pb-0.5 transition-colors hover:border-foreground hover:text-foreground">
							<span class="toggle-closed">Show {readingRest.length} more</span>
							<span class="toggle-open">Show less</span>
						</summary>

						<ul class="mt-6 border-t border-border">
							{readingRest.map((item) => (
								<li class="border-b border-border py-4">
									<a
										href={item.url}
										target="_blank"
										rel="noreferrer"
										class="link-underline text-[0.9375rem] font-medium leading-snug"
									>
										{item.title}
									</a>
									<p class="label-meta mt-1.5">{item.source}</p>
								</li>
							))}
						</ul>
					</details>
				)
			}
		</section>

		<section id="contact" class="rule-top py-16">
			<p class="label-eyebrow">Let&apos;s connect</p>
			<p
				class="mt-6 max-w-2xl text-balance font-display text-2xl font-semibold leading-snug tracking-tight sm:text-3xl"
			>
				Always happy to talk about backend platforms, agentic systems, or a good distributed
				systems war story.
			</p>

			<div class="mt-8 flex flex-wrap items-center gap-x-8 gap-y-3 text-sm font-medium">
				<a href="mailto:anikesh.kamath@gmail.com" class="link-underline">Email</a>
				<a
					href="https://www.linkedin.com/in/anikeshk/"
					target="_blank"
					rel="noreferrer"
					class="link-underline text-muted-foreground"
				>
					LinkedIn
				</a>
				<a
					href="https://github.com/anikeshk"
					target="_blank"
					rel="noreferrer"
					class="link-underline text-muted-foreground"
				>
					GitHub
				</a>
			</div>
		</section>
	</main>

	<Footer />
</Main>
